module {
  public type UserId = Principal;
  public type MomentId = Text; // UUID v4 Text for global uniqueness
  public type FolderId = Nat;
  public type MediaId = Nat;
  public type CommentId = Nat;
  public type Timestamp = Int;
};
